from setuptools import setup, find_packages

setup(
    name='mosaicpy',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        'requests',  # TODO Add any dependencies required by your package
    ],
    # Other metadata and configurations
)
